spring:
  application:
    name: dschang-signal
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5432/dschang-signal
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  base-path: /api